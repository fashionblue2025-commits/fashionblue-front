version: '3.8'

services:
  frontend:
    build: .
    ports:
      - "3000:80"
    environment:
      - VITE_API_URL=http://localhost:8080/api/v1
    restart: unless-stopped
    networks:
      - fashion-blue-network

networks:
  fashion-blue-network:
    driver: bridge
